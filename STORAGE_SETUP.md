# 비공개 조회 이력 저장

1. 기존 Supabase 프로젝트의 SQL Editor에서 `supabase_setup.sql`을 실행합니다.
   기존 validation_history와 별개로 `kakao_route_history` 테이블과 비공개 `kakao-route-results` 버킷을 생성합니다.
2. 카카오 노선 앱의 Streamlit Settings → Secrets에 다음 항목을 최상위에 추가합니다.
   기존 API 키와 APP_PASSWORD는 그대로 유지합니다.

```toml
SUPABASE_URL = "https://PROJECT_ID.supabase.co"
SUPABASE_SERVICE_ROLE_KEY = "기존 프로젝트의 service_role 키"
```

키는 GitHub에 올리지 않습니다. 테이블은 익명·일반 사용자 접근을 차단하고 서버가 관리자 키로 접근합니다.
버킷도 비공개입니다. 기존 프로젝트에 광범위한 Storage 접근 정책이 있다면 새 버킷도 허용하는지 확인해야 합니다.
웹앱의 접속 비밀번호를 아는 사람들은 전체 이력을 공유합니다.

3. 앱에서 엑셀 조회 후 조회 이력에 나타나는지 확인합니다. 새 세션에서도 내려받을 수 있어야 합니다.
4. 조회 이력 → 기존 PC 조회 결과 복원에 원본 ZIP을 올리고 복원 버튼을 누릅니다.
   results 폴더의 결과 엑셀만 저장합니다. DB에 들어 있던 주소·경로 캐시는 복원하지 않으며 SQLite 캐시로 남습니다.
   복원 시 이력의 생성 시각은 복원 시각이며 원래 날짜는 파일명에서 확인할 수 있습니다.

클라우드 연결 정보가 없으면 이전과 동일하게 로컬에 저장하고 화면에서 이를 알립니다.
연결 정보가 잘못되거나 저장소가 응답하지 않으면 오류를 표시합니다. 결과 엑셀은 다운로드하여 보관할 수 있습니다.
클라우드의 목록 삭제는 복구 가능한 소프트 삭제입니다. 원본 파일은 보존됩니다.
별도 백업, 저장 용량과 서비스 상태 관리는 여전히 필요합니다.
